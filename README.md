# Telegram Trading Bot

## Команди:
- `/start` – запуск
- `/signal` – RSI + EMA + патерн
- `/price` – поточна ціна BTC
- `/news` – останні новини

## Встановлення:
```
pip install -r requirements.txt
python main.py
```